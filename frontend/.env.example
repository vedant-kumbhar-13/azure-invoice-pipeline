# Local development only — copy to .env.local
# BUG-C6: For production builds, edit .env.production instead.
#         A production build WITHOUT .env.production silently calls localhost and fails.
VITE_API_URL=http://localhost:8001
